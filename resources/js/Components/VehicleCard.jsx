import {Link} from '@inertiajs/react';

const date = value => new Date(`${value.slice(0, 10)}T00:00:00`).toLocaleDateString('id-ID', { day: 'numeric', month: 'long', year: 'numeric' });

export default function VehicleCard({vehicle}){const image=vehicle.images?.find(i=>i.is_primary)||vehicle.images?.[0],booking=vehicle.unavailable_booking;return <article className="overflow-hidden rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><div className="relative flex h-44 items-center justify-center bg-slate-200">{image?<img className="h-full w-full object-cover" src={`/storage/${image.file_path}`} alt={`${vehicle.brand} ${vehicle.model}`}/>:<span className="text-sm text-slate-500">Foto kendaraan</span>}{booking&&<span className="absolute right-3 top-3 rounded-full bg-red-600 px-3 py-1 text-xs font-bold text-white">Tidak tersedia</span>}</div><div className="p-5"><p className="text-sm text-indigo-600">{vehicle.category.name}</p><h3 className="mt-1 text-lg font-bold">{vehicle.brand} {vehicle.model}</h3><p className="mt-2 text-sm text-slate-500">{vehicle.transmission} · {vehicle.seat_capacity} kursi · {vehicle.fuel_type}</p>{booking&&<p className="mt-3 rounded-lg bg-red-50 p-3 text-xs font-semibold leading-5 text-red-700">Tidak tersedia dari {date(booking.start_datetime)} s/d {date(booking.end_datetime)}.</p>}<div className="mt-4 flex items-center justify-between"><strong>Rp {Number(vehicle.daily_price).toLocaleString('id-ID')}<span className="text-sm font-normal">/hari</span></strong><Link className="text-sm font-semibold text-indigo-600" href={route('cars.show',vehicle.id)}>Detail</Link></div></div></article>}
