import { Head, router } from '@inertiajs/react';
import PublicLayout from '@/Layouts/PublicLayout';
import VehicleCard from '@/Components/VehicleCard';
export default function Index({ vehicles, categories, filters }) { const update=(key,value)=>router.get(route('cars.index'),{...filters,[key]:value},{preserveState:true,replace:true}); return <PublicLayout><Head title="Daftar Mobil"/><main className="mx-auto max-w-7xl px-6 py-12"><p className="text-sm font-semibold text-indigo-600">KATALOG KENDARAAN</p><h1 className="mt-2 text-4xl font-bold">Pilih mobil Anda</h1><div className="mt-8 grid gap-3 rounded-xl bg-white p-4 shadow-sm md:grid-cols-3"><select className="rounded border-slate-300" value={filters.category||''} onChange={e=>update('category',e.target.value)}><option value="">Semua kategori</option>{categories.map(c=><option key={c.slug} value={c.slug}>{c.name}</option>)}</select><select className="rounded border-slate-300" value={filters.transmission||''} onChange={e=>update('transmission',e.target.value)}><option value="">Semua transmisi</option><option value="AUTOMATIC">Automatic</option><option value="MANUAL">Manual</option></select><select className="rounded border-slate-300" value={filters.seats||''} onChange={e=>update('seats',e.target.value)}><option value="">Semua kapasitas</option><option value="4">4+ kursi</option><option value="6">6+ kursi</option></select></div><div className="mt-8 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{vehicles.data.map(v=><VehicleCard key={v.id} vehicle={v}/>)}</div>{vehicles.data.length===0&&<p className="mt-8 text-slate-500">Tidak ada kendaraan yang cocok.</p>}</main></PublicLayout>; }
