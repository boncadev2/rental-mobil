import {Head, Link, router} from '@inertiajs/react';
import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';

const money = value => `Rp ${Number(value || 0).toLocaleString('id-ID')}`;

export default function Financial({filters, summary, payments, expenses}) {
    const apply = event => {
        event.preventDefault();
        router.get(route('admin.reports.financial'), Object.fromEntries(new FormData(event.currentTarget)), {preserveState: true});
    };
    const charges = [['Denda keterlambatan', summary.late_fee], ['Kerusakan kendaraan', summary.damage_fee], ['Kekurangan bahan bakar', summary.fuel_fee], ['Kebersihan', summary.cleaning_fee]];

    return <AuthenticatedLayout header="Laporan Keuangan"><Head title="Laporan Keuangan"/><div className="mx-auto max-w-7xl"><div className="flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-2xl font-black">Laporan keuangan</h1><p className="mt-1 text-slate-500">Pendapatan, piutang, tagihan tambahan, dan pengeluaran maintenance.</p></div><form onSubmit={apply} className="flex flex-wrap gap-2"><input name="from" type="date" defaultValue={filters.from} className="rounded-xl border-slate-200"/><input name="to" type="date" defaultValue={filters.to} className="rounded-xl border-slate-200"/><button className="rounded-xl bg-indigo-600 px-4 py-2 font-bold text-white">Terapkan</button></form></div><div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-5"><div className="rounded-2xl bg-indigo-600 p-5 text-white"><p className="text-sm font-semibold opacity-80">Pembayaran diterima</p><p className="mt-3 text-2xl font-black">{money(summary.payment_total)}</p><p className="mt-1 text-sm opacity-80">{summary.payment_count} transaksi berhasil</p></div><div className="rounded-2xl border border-amber-200 bg-amber-50 p-5 shadow-sm"><p className="text-sm font-semibold text-amber-800">Piutang aktif</p><p className="mt-3 text-2xl font-black text-amber-900">{money(summary.receivable_total)}</p><p className="mt-1 text-xs text-amber-700">{summary.receivable_count} booking belum lunas</p></div><div className="rounded-2xl bg-white p-5 shadow-sm"><p className="text-sm font-semibold text-slate-500">Pengeluaran maintenance</p><p className="mt-3 text-2xl font-black text-red-600">{money(summary.maintenance_total)}</p></div><div className="rounded-2xl bg-white p-5 shadow-sm"><p className="text-sm font-semibold text-slate-500">Pendapatan bersih</p><p className="mt-3 text-2xl font-black">{money(summary.net_total)}</p><p className="mt-1 text-xs text-slate-400">Pembayaran diterima - maintenance</p></div><div className="rounded-2xl bg-white p-5 shadow-sm"><p className="text-sm font-semibold text-slate-500">Biaya tambahan tercatat</p><p className="mt-3 text-2xl font-black">{money(summary.additional_total)}</p><p className="mt-1 text-xs text-slate-400">Belum otomatis dianggap diterima</p></div></div><div className="mt-6 grid gap-5 lg:grid-cols-3"><section className="rounded-2xl bg-white p-6 shadow-sm"><h2 className="font-black">Rincian biaya tambahan</h2><div className="mt-4 divide-y">{charges.map(([label, amount]) => <p key={label} className="flex justify-between py-3 text-sm"><span>{label}</span><b>{money(amount)}</b></p>)}<p className="flex justify-between pt-4 font-black"><span>Total</span><span>{money(summary.additional_total)}</span></p></div></section><section className="overflow-hidden rounded-2xl bg-white shadow-sm lg:col-span-2"><div className="border-b p-6"><h2 className="font-black">Pengeluaran maintenance terbaru</h2></div>{expenses.length ? <div className="divide-y">{expenses.map(expense => <div key={expense.id} className="flex justify-between gap-4 p-4 text-sm"><div><b>{expense.type.name} · {expense.vehicle.brand} {expense.vehicle.model}</b><p className="mt-1 text-slate-500">{new Date(expense.maintenance_date).toLocaleDateString('id-ID')} · {expense.workshop || 'Tanpa nama bengkel'}</p></div><b className="text-red-600">-{money(expense.cost)}</b></div>)}</div> : <p className="p-6 text-slate-500">Belum ada pengeluaran maintenance pada periode ini.</p>}</section></div><section className="mt-6 overflow-hidden rounded-2xl bg-white shadow-sm"><div className="border-b p-6"><h2 className="font-black">Transaksi pembayaran berhasil</h2></div>{payments.data.length ? <div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-slate-500"><tr><th className="p-4">Waktu</th><th className="p-4">Booking</th><th className="p-4">Pelanggan</th><th className="p-4">Metode</th><th className="p-4 text-right">Nilai</th></tr></thead><tbody>{payments.data.map(payment => <tr key={payment.id} className="border-t"><td className="p-4">{payment.paid_at ? new Date(payment.paid_at).toLocaleString('id-ID') : '-'}</td><td className="p-4"><Link href={route('admin.bookings.show', payment.booking_id)} className="font-bold text-indigo-600">{payment.booking?.booking_code}</Link></td><td className="p-4">{payment.booking?.customer?.full_name || '-'}</td><td className="p-4">{payment.method}</td><td className="p-4 text-right font-bold">{money(payment.amount)}</td></tr>)}</tbody></table></div> : <p className="p-6 text-slate-500">Belum ada pembayaran berhasil pada periode ini.</p>}</section></div></AuthenticatedLayout>;
}
