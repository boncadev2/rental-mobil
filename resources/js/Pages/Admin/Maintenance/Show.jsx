import { Head, Link } from '@inertiajs/react';
import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';

const money = value => `Rp ${Number(value || 0).toLocaleString('id-ID')}`;

export default function Show({ vehicle, schedules, records }) {
    return <AuthenticatedLayout header="Detail Maintenance"><Head title={`Maintenance ${vehicle.brand} ${vehicle.model}`} /><Link href={route('admin.maintenance.index')} className="text-sm font-bold text-indigo-600">← Semua kendaraan</Link><div className="mt-4 flex flex-wrap items-start justify-between gap-4"><div><h1 className="text-2xl font-black">{vehicle.brand} {vehicle.model}</h1><p className="mt-1 text-slate-500">{vehicle.license_plate} · Odometer {Number(vehicle.current_odometer).toLocaleString('id-ID')} km</p></div><Link href={route('admin.maintenance.create',{vehicle:vehicle.id})} className="rounded-xl bg-indigo-600 px-4 py-3 font-bold text-white">+ Catat maintenance</Link></div><section className="mt-6 overflow-hidden rounded-2xl bg-white shadow"><div className="border-b p-5"><h2 className="font-black">Riwayat maintenance kendaraan</h2></div><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-slate-500"><tr><th className="p-4">Tanggal</th><th className="p-4">Pekerjaan</th><th className="p-4">Odometer</th><th className="p-4">Bengkel</th><th className="p-4 text-right">Biaya</th></tr></thead><tbody>{records.data.length?records.data.map(record=><tr key={record.id} className="border-t"><td className="p-4">{new Date(record.maintenance_date).toLocaleDateString('id-ID')}</td><td className="p-4"><b>{record.type.name}</b>{record.description&&<p className="mt-1 text-xs text-slate-500">{record.description}</p>}</td><td className="p-4">{Number(record.odometer).toLocaleString('id-ID')} km</td><td className="p-4">{record.workshop||'-'}</td><td className="p-4 text-right font-bold text-red-600">{money(record.cost)}</td></tr>):<tr><td colSpan="5" className="p-10 text-center text-slate-500">Belum ada riwayat maintenance.</td></tr>}</tbody></table></div></section><section className="mt-6 overflow-hidden rounded-2xl bg-white shadow"><div className="border-b p-5"><h2 className="font-black">Jadwal berikutnya</h2></div><div className="divide-y">{schedules.length?schedules.map(schedule=><div key={schedule.id} className="flex flex-wrap justify-between gap-3 p-5"><div><b>{schedule.maintenance_type.name}</b><p className="mt-1 text-sm text-slate-500">{schedule.next_service_date?`Tanggal: ${new Date(schedule.next_service_date).toLocaleDateString('id-ID')}`:'Tidak ada jadwal tanggal'}{schedule.next_service_odometer&&` · ${Number(schedule.next_service_odometer).toLocaleString('id-ID')} km`}</p></div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">{schedule.status}</span></div>):<p className="p-5 text-slate-500">Belum ada jadwal maintenance.</p>}</div></section></AuthenticatedLayout>;
}
