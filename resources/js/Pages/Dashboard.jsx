import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';import {Head} from '@inertiajs/react';
export default function Dashboard({metrics={}}){const cards=[['Total booking',metrics.bookings??0],['Pendapatan',`Rp ${Number(metrics.revenue??0).toLocaleString('id-ID')}`],['Mobil digunakan',metrics.vehiclesInUse??0],['Maintenance alert',metrics.maintenanceDue??0]];return <AuthenticatedLayout header="Dashboard"><Head title="Dashboard"/><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-4">{cards.map(([t,v],i)=><div key={t} className={`rounded-2xl p-6 shadow-sm ${i===0?'bg-indigo-600 text-white':'bg-white'}`}><p className="text-sm opacity-70">{t}</p><p className="mt-3 text-3xl font-bold">{v}</p></div>)}</div><div className="mt-6 rounded-2xl bg-white p-7 shadow-sm"><h3 className="font-bold">Ringkasan operasional</h3><p className="mt-2 text-sm text-slate-500">Gunakan menu Booking dan Kendaraan untuk melihat detail operasional.</p></div></AuthenticatedLayout>}
