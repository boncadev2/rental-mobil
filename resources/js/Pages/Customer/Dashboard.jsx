import { Head, Link } from '@inertiajs/react';
import AuthenticatedLayout from '@/Layouts/AuthenticatedLayout';

const money = (value) => `Rp ${Number(value).toLocaleString('id-ID')}`;
export default function Dashboard({ metrics, recentBookings, customerPhoneVerified, customerKtpVerified }) { const cards = [['Total Booking', metrics.bookings, 'Semua riwayat sewa'], ['Sedang Aktif', metrics.active, 'Booking terkonfirmasi'], ['Menunggu Bayar', metrics.pending, 'Selesaikan pembayaran'], ['Total Transaksi', money(metrics.spending), 'Akumulasi pesanan']]; const verificationComplete = customerPhoneVerified && customerKtpVerified; const verificationMessage = customerKtpVerified ? 'KTP sudah terverifikasi. Verifikasi nomor WhatsApp untuk menyelesaikan verifikasi akun.' : 'Unggah KTP dan verifikasi nomor WhatsApp untuk menjadi pelanggan VERIFIED.'; return <AuthenticatedLayout header="Dashboard Saya"><Head title="Dashboard Pelanggan" /><div className="flex flex-wrap items-center justify-between gap-4"><div><h1 className="text-2xl font-black">Selamat datang kembali</h1><p className="mt-1 text-slate-500">Kelola booking mobil Anda dalam satu tempat.</p></div></div>{!verificationComplete && <div className="mt-6 flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-amber-200 bg-amber-50 p-5 text-amber-900"><div><b>Verifikasi akun Anda</b><p className="mt-1 text-sm">{verificationMessage}</p></div><Link href={route('profile.edit')} className="rounded-xl bg-amber-500 px-4 py-2.5 text-sm font-bold text-white">Verifikasi akun</Link></div>}<div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label, value, caption]) => <div key={label} className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><p className="text-sm font-semibold text-slate-500">{label}</p><p className="mt-3 text-2xl font-black text-slate-900">{value}</p><p className="mt-2 text-xs text-slate-400">{caption}</p></div>)}</div><section className="mt-8 rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><div className="flex items-center justify-between"><h2 className="text-lg font-black">Booking terbaru</h2><Link href={route('my-bookings.index')} className="text-sm font-bold text-indigo-600">Lihat semua</Link></div>{recentBookings.length ? <div className="mt-5 divide-y">{recentBookings.map(booking => <div key={booking.id} className="flex flex-wrap items-center justify-between gap-3 py-4"><div><p className="font-bold">{booking.vehicle.brand} {booking.vehicle.model}</p><p className="mt-1 text-sm text-slate-500">{booking.booking_code} · {new Date(booking.start_datetime).toLocaleDateString('id-ID')}</p></div><div className="text-right"><p className="font-bold">{money(booking.total_amount)}</p><span className="text-xs font-bold text-indigo-600">{booking.status}</span></div></div>)}</div> : <p className="mt-5 text-slate-500">Belum ada booking. Mulai pilih mobil untuk perjalanan Anda.</p>}</section></AuthenticatedLayout>; }
